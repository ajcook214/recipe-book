{
  "name": "recipe-book",
  "private": true,
  "type": "module",
  "description": "Offline-first personal recipe book and shopping list. No build step.",
  "scripts": {
    "test": "node --test \"test/**/*.test.js\"",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
